edits:
- messages + re-explanations should have a decaying effect on the maturity of the skill. i.e. if i demonstrate strong understanding 10 turns ago, i want it to still have an impact on my score, and if i don't explain something well in the current turn, i don't want it to completely tank my score.
- can we create our own hosted UI instead of the default popup for new brain, maybe something like a custom modal to align with the UI theme
- can you make the scoring contiuous distribution, maybe create a tool for claude to compute, instead of letting the LLM compute the score end-to-end
- it seems like we aren't able to add more notes for more nodes after the initial brain creation, can we ensure that we're still able to continue to add to our "brain"
- node suggestions: it'd also be great if we can add dotted nodes on the perimeter of our brain that can "improve the coverage" for our brain, maybe from the nodes above 60-70% mastery, then we'd be able to encourage learning and building off of something that the user has demonstrated mastery for.

- the neuron map seems a little cluttered right now, can you make it so that only the nodes/bubbles near the cursor within some threshold radius expand to show the node text, otherwise, collapse the node to only colored nodes w/o text

- now that we have multi-brain backend capacity, can we also attempt to have an overview / dashboard tab that shows all the brains in vector space, and dotted lines to connect the ones that might be interconnected (i.e. math to personal finance, physics to sports, etc.) -it'd be really cool if this could be shaped like a brain so our front page really catches peoples eyes.
