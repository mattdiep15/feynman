v3 changes:
- for the multibrain preview, i was more thinking like dotted lobe clusters, with the nodes inside, that way you could even overlay a translucent or transparent brain diagram over the nodes similar to neuron network firing animations instead of grouping them all under big categories. it's like unfocused, and simply slowly rotating when no brain is selected, and then when you do select a brain from the left brain menu, it focuses on it and any dotted related connections.
- the hover bubbles is freezing after like 3 seconds of hovering around, i was thinking more that the nodes would shrink and expand as well upon hovering to make the experience more dynamic. otherwise they're all the same size dots/smaller circles of solid color, and then when you hover, it expands the node (and thus the graph), filling the nodes with their text.
- for the scoring metric it seems a little heavy for recency, maybe let's lower our gamma. from one simple error, i went from 62% to 20%. also, can we filter out/bypass scoring if the user's input is neutral / irrelevant? like say i ask "can you test me on income", i dont' want my score to go down, and similar for like filler responses like "yeah for sure!" and accidentally hitting enter before finishing what i'm about to say. furthermore, i don't want responses to be considered as the wholistic understanding. that would put too much weight on any singular response, so maybe that further justifies lowering the gamma, but take for example the @docs/draft/example_conversation.md . this is a case where i'm chatting with the agent, but answering the follow up (logically) ignores any previous basics that we had already covered, since it's at the very least already been said at the beginning of the converstaion, and yet in the response message, it says that i did not define income itself as its most basic level. this seems quite unproductive, so maybe we should start scoring entire chat sessions as one, and keep a running score during the chat sessions before committing it to mastery when the session ends / the user leaves the particular node and restarts the conversation. that way the context is not lost in lookback k=1.
- @docs/draft/settings.md
- @docs/draft/landing.md
