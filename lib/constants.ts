// Rule 5 — userId is a hardcoded constant, no auth. brainId is now dynamic
// (per request); DEFAULT_BRAIN_ID is the fallback so existing data keeps working.
export const USER_ID = 'demo';
export const DEFAULT_BRAIN_ID = 'finance';
export const CLAUDE_MODEL = 'claude-sonnet-4-6';
